import { useState } from "react";
import { v4 } from "uuid";

const Edit = ({}) => {
  return (
    <div>
      <button className="buy" style={{ fontSize: 22 }}>
        加入購物車
      </button>
    </div>
  );
};

export default Edit;
